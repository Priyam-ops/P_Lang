// PLang Test File - Syntax Highlighting Demo

// Variable declarations
x is 10
y is 20
name is "PLang"
flag is yes

// Arithmetic operations
sum is x + y
difference is y - x
product is x * y
quotient is y / x

// Display results
show "Welcome to PLang!"
show name
show sum
show difference

// Conditional statements
if sum > 25
    show "Sum is greater than 25"
    result is "high"
else
    show "Sum is 25 or less"
    result is "low"

// Comparison operators
if x = 10
    show "x equals 10"

if y > x
    show "y is greater than x"

if x <= y
    show "x is less than or equal to y"

// Loop example
counter is 0
while counter < 5
    show counter
    counter++

// Input example
// take userInput
// show userInput

// Increment/decrement
x++
y--

// String operations
greeting is "Hello, "
message is greeting + name
show message

// Boolean operations
if flag = yes
    show "Flag is true"

// More complex expressions
complex is (x + y) * 2 - 1
show complex

// Program commands
clear
help
